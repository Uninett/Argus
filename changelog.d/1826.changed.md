Tailwind CSS intermediate files (theme CSS, config snippets) are no longer tracked in git.

NOTES:
- Local theme customizations via `DAISYUI_THEMES` will no longer dirty the
  working tree.
- Use `make tailwind-config tailwind` after changing themes or settings.
- Use `make setup-tailwind` to bootstrap CSS from scratch (fresh clone or CI).
