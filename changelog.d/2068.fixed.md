When combining filters in order to check if something is on maintenance or
should trigger a notification, cruft stored by default could prevent the
combination from hitting anything.

A filter like this:

    {"open": null, "acked": null, "maxlevel": 5}

should be equivalent to an empty filter, this:

    {}

.. so now we wash the filter just before it is stored.
