Fixed a problem with `maxlevel` in the filter box caused by the default of an
HTML range/input defaulting to the middle value if no value is given. It now
defaults to the maximum level, "5", as it should. This should cut down on
maxlevel suddenly flipping to "3" for apparently no reason.
