Argus can now be served from a sub-path of a domain, for instance `https://example.org/argus/`, by setting the `ARGUS_FRONTEND_SUBURL` environment variable. The reverse proxy in front must pass the sub-path through unchanged, since Argus generates URLs that contain it.
