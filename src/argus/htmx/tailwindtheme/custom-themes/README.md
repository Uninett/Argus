# Custom themes

This directory is for custom DaisyUI themes added by users and downstream
projects. Files can be added manually or generated by `manage.py tailwind_config`
(or `make tailwind-config`) from dict entries in the `DAISYUI_THEMES` setting.

This directory is gitignored. Official Argus themes belong in `../themes/`.
